# AZERIOID Stack Manager web app

Laravel application that runs as `caddy` / `www-data`. It never executes package managers or `systemctl` itself — privileged work goes through the broker.

See the repository root [README.md](../README.md) for architecture, security model, and install.
