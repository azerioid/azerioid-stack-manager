; AZERIOID Stack Manager dedicated PHP-FPM pool (example; install.sh generates the live file).
; Isolated from www.conf. shell_exec / system / exec stay disabled.
; Do not add proc_open to disable_functions here.

[azerioid-panel]
user = caddy
group = caddy
listen = /run/php/azerioid-panel.sock
listen.owner = caddy
listen.group = caddy
listen.mode = 0660

pm = ondemand
pm.max_children = 4
pm.process_idle_timeout = 10s
pm.max_requests = 200

php_admin_value[disable_functions] = passthru,exec,shell_exec,system,chroot,chgrp,chown,ini_alter,ini_restore
php_admin_flag[expose_php] = off
php_admin_value[open_basedir] = /usr/local/lib/azerioid-panel/web:/tmp:/dev/urandom:/usr/bin/sudo:/var/log/azerioid-panel
php_admin_value[sys_temp_dir] = /usr/local/lib/azerioid-panel/web/storage/framework/tmp
php_admin_value[upload_tmp_dir] = /usr/local/lib/azerioid-panel/web/storage/framework/tmp
php_admin_value[session.save_path] = /usr/local/lib/azerioid-panel/web/storage/framework/sessions
php_admin_flag[log_errors] = on
php_admin_value[error_log] = /var/log/azerioid-panel/php-fpm.log
